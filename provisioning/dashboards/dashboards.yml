apiVersion: 1

providers:
  - name: 'AWS Cost Explorer examples'
    orgId: 1
    folder: 'AWS Cost Explorer'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
